package src.main.scala

import scala.collection.immutable.TreeSet
import java.io._

object PunPal extends App {
//  val dictFile = "/usr/share/dict/american-english"
//  val dict = Source.fromFile(dictFile).getLines()
//  val dictTrie = dict.foldLeft(new TreeSet[String])(_ + _)
//    
//  val puns = dict.map(x => (x, Pun.punScores(dictTrie, x).filter(case (s, x) => s > threshold)))
}